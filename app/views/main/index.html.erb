Hai there
<%= render :partial => "page_setup", :locals => {:seeds => @seeds} %>

<%= link_to "Login", {:controller => :admins, :action => :login}, :id => "haro" %>

<% if flash[:message] %>
<%= flash[:message] %>

<br/>
<% end %>	
<% @seeds.each do |s| %>
	<div id="seedcontainer<%=s.id.to_s%>">
		<div id="thumbdiv<%=s.id.to_s%>"><%= image_tag "rb.png", :id => "thumbnailimg#{s.id.to_s}" %></div> 
		<div id="seedesc<%=s.id.to_s%>"></div>
		<div id="pulse<%=s.id.to_s%>"></div>
	</div>
	
	
<% end %>